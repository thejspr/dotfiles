#!/usr/bin/env bash

# quickly find a branch, using any part of the name to narrow it down further.
# requires "peco" (`brew install peco`) installed

set -e

git checkout $(git branch | peco --select-1 --layout=bottom-up --query=$1)
