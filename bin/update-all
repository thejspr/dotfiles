#!/usr/bin/env zsh

if uname -a | grep -q 'MANJARO';
then
  sudo pacman -Syu
  yay -Syu
else
  sudo apt update
  sudo apt upgrade
  sudo apt autoremove -f
fi

nvim +PlugUpdate +qall

gem install solargraph

# omz update
