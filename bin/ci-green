set -e

while(true); do
  var="`hub ci-status`"

  if [ $var = 'success' ]; then
    echo "CI passed"
    exit 0
  elif [ $var = 'failure' ]; then
    echo "CI Failed!"
    exit -1
  elif [ $var = 'pending' ]; then
    sleep 10
  fi
done
