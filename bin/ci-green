#!/usr/bin/env bash

set -e

while(true); do
  status=$(hub ci-status)

  if [ $status = 'success' ]; then
    echo "CI passed"
    exit 0
  elif [ $status = 'failure' ]; then
    echo "CI Failed!"
    exit -1
  elif [ $status = 'pending' ]; then
    echo "Pending"
    sleep 10
  fi
done
