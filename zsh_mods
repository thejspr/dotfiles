export HISTCONTROL=ignoreboth:erasedups
export EDITOR='nvim'
export BUNDLER_EDITOR='nvim'
eval "$(rbenv init - --no-rehash)"
export PATH=/usr/local/bin:/usr/local/share/npm/bin:$PATH
export PATH=./bin:~/bin:/Applications/Postgres.app/Contents/Versions/9.6/bin:$PATH
export PATH="$PATH:`yarn global bin`"
export PATH="$PATH:$PWD/bin"
export SHELL=/usr/local/bin/zsh

# prezto customizations
autoload zmv
bindkey -e
# unalias g
compdef g=git
fpath=(/usr/local/share/zsh-completions $fpath)
alias tar='/usr/local/bin/gtar'

# Ruby
export RUBY_BUILD_CACHE_PATH=true
export RUBY_HEAP_SLOTS_INCREMENT=10000
export RUBY_HEAP_SLOTS_GROWTH_FACTOR=1.8
export RUBY_GC_MALLOC_LIMIT=59000000
export RUBY_HEAP_FREE_MIN=100000

# hub alias
eval "$(hub alias -s)"

# https://github.com/rupa/z
. /usr/local/etc/profile.d/z.sh

# Alias and function definitions.
. ~/code/dotfiles/aliases

fancy-ctrl-z () {
  if [[ $#BUFFER -eq 0 ]]; then
    BUFFER="fg"
    zle accept-line
  else
    zle push-input
    zle clear-screen
  fi
}
zle -N fancy-ctrl-z
bindkey '^Z' fancy-ctrl-z

source ~/.secrets

source "$HOME/.vim/plugged/gruvbox/gruvbox_256palette.sh"
