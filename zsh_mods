stty -ixon
# if [ $(hostname) = "jesper.local" ]; then
  unalias g
  . ~/.mac_settings
# else
  # . ~/.nix_settings
# fi

export RUBY_BUILD_CACHE_PATH=true
eval "$(rbenv init - --no-rehash)"
export PATH=./bin:~/bin:$PATH

# hub alias
# DISABLED: clashes with git-whistles
# eval "$(hub alias -s)"

# prezto customizations
autoload zmv
bindkey -e
compdef g=git
fpath=(/usr/local/share/zsh-completions $fpath)
alias tar='/usr/local/bin/gtar'

# Ruby
export RUBY_HEAP_SLOTS_INCREMENT=10000
export RUBY_HEAP_SLOTS_GROWTH_FACTOR=1.8
export RUBY_GC_MALLOC_LIMIT=59000000
export RUBY_HEAP_FREE_MIN=100000

# Alias and function definitions.
. ~/code/dotfiles/aliases

# work
eval "$($HOME/.ht/bin/ht init -)"
export DISABLE_GROWL=YES
export INTEGRITY_HOST=localhost
export APP_DOMAIN=localhost
# export SPLIT_CSS_FOR_IE=true

# JAVA
export JAVA_HOME="$(/usr/libexec/java_home)"

#libsass
export SASS_LIBSASS_PATH=~/Code/HouseTrip/libsass
export PATH=$PATH:/Users/jesper/Code/HouseTrip/sassc/bin

# docker
export DOCKER_HOST=tcp://localhost:4243
