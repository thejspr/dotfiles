# msc
alias youtube-dl='noglob youtube-dl'

#testing
alias pam='polyamory'

export EDITOR=vim
alias time='/usr/bin/time'

#vim
alias vim='mvim -v'
alias tmux="TERM=screen-256color-bce tmux"

alias cuc='cucumber'

#evently
alias risky='RISKY=true'
alias dq='DO_NOT_QUIT_BROWSER=true'
alias rsq='ps -ef | grep resque'
local_broadcast() { DO_NOT_QUIT_BROWSER=true RAILS_ENV=production rake broadcast:work QUEUE="$1" }

#broadcast spec runners
rsh() { be rspec spec/broadcast/sites/helpers/"$1"_helper_spec.rb"$2" }
rss() { LIVE=true be rspec spec/broadcast/sites/"$1"_spec.rb"$2" }

# tail server rails log
tail_server() { ssh -t "$1" tail -f /var/www/bullseye/production/current/log/production.log }
tail_test_server() { ssh -t "$1" tail -f /var/www/bullseye/testing/current/log/production.log }

#conviniences
alias -g A='|ack'
alias -g L='|less'
alias -g V='|vim -'
alias -g M='|mvim -'
alias -g G='|grep'
alias -g N='&>/dev/null&'
alias -g W='|wc'

#git
g() {
  if [[ $# == '0' ]]; then
    command hub status -sb
  else
    command hub "$@";
  fi
}
grmb() { git push origin :"$@"; git branch -D "$@" }

alias gd='g d'
alias gl='g l'
alias gr='g r'

#bundler
alias bs='bundle show'

#rails
alias migrate='rake db:migrate && rake db:test:clone_structure'
alias rs='rails server thin'
alias rc='rails console'
alias rg='rails generate'

reset_db() {
  rake db:drop && rake db:create && migrate && rake db:seed
}

#logs, #rails
alias devlog='tail -f ./log/development.log'
alias testlog='tail -f ./log/test.log'

#ctags
alias rctags='ctags -R --exclude=.git --exclude=log --exclude=public *'

#ruby
alias rb='rbenv'
alias rv='ruby -v'
alias wr='ps aux | grep ruby'

#rake
alias rt='rake -T'

#heroku
alias h="heroku"
# complete -o default -o nospace -F _heroke h
hpush() { git add -A; git commit -m "$*"; git push heroku master; }

#servers
myip() { elinks -dump http://checkip.dyndns.org:8245/ }

#msc
alias mx='chmod +x'
alias cd..='cd ..'
alias grep='grep --color=auto'

# system and files editing
alias ea='vim ~/.aliases'
alias ez='vim ~/Code/dotfiles/zshrc'
alias ez_='vim ~/Code/dotfiles/_zshrc'
alias sr='source ~/.zshrc'
